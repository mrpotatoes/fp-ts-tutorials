/**
 * A logger function that will:
 *    - Completely composed (pipe(), flow())
 *    - Only logs the full stack trace if there is global flag that is set to true (Reader)
 *    - Logs to the terminal (IO)
 *    - Can handle pushing something to an API (TaskEither?)
 */